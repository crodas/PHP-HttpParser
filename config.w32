// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("webserver", "for webserver support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("webserver", "enable webserver support", "no");

if (PHP_WEBSERVER != "no") {
	EXTENSION("webserver", "webserver.c");
}

